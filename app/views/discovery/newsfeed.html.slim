.row
  .span4
    .wrapper= render 'menu'
  .span8
    .wrapper
      h1= tr('News feed')
      - unless @activities.blank?
        .list_view
          = will_paginate @activities
          = render @activities
          = will_paginate @activities
      - else
        p.muted= tr('No activities yet.')
