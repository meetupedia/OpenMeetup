= div_for membership.user do
  - if can? :destroy, membership
    .pull-right= link_to 'Törlés', membership, :method => :delete, :confirm => 'Biztosan törlöd?', :class => 'btn btn-danger btn-small'
  .avatar= user_avatar membership.user
  = link_to membership.user.name, membership.user
