.wrapper
  .pull-right= link_to tr('Next step'), root_path, :class => 'btn btn-primary'

  h1= tr('What are your interests?')
  'hello
  #interest_taggings= render :partial => 'interest_taggings/button', :collection => Interest.order('name ASC'), :as => :interest

  .pull-right= link_to tr('Next step'), root_path, :class => 'btn btn-primary'
  br
