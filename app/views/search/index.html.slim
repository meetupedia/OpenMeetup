h1
  = tr('Results')
  ' :
  = params[:q]

.row
  .span7
    - unless @groups.blank?
      h2= tr('Groups')
      .list_view= render @groups
    - else
      p= tr('Not found.')
  .span4.offset1
    - unless @events.blank?
      h2= tr('Events')
      .list_view= render @events
    - else
      p= tr('Not found.')
